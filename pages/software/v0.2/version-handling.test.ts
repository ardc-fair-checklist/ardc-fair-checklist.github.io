import { describe } from 'vitest';
import { expect } from 'vitest';
import { test } from 'vitest';

describe('Version handling', () => {
    describe('If there is a newer version of the topic,', () => {
        test.todo('the app should show a banner telling the user there is a newer version available', () => {
            expect(true).toBe(true);
        });
    });
});
